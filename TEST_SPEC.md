# TEST_SPEC.md — kansetsu-koyomi

<!-- scaffold template v1.24.0 から展開(2026-09-02) -->

## 実行規約

- `python -m pytest -q` を出荷データ(`data/*.json`)に対する判定に使う。マーカー: `unit` / `validation`
- `npm test`(vitest)を暦・図の骨格・フッタ規約の判定に使う
- `node harness/smoke.mjs` を実ブラウザ検品に使う。**目で見なければ分からない性質は
  テストで代替せず、この検品と目視を完了条件に置く**(HC-041)
- `npm run verify` は typecheck → vitest → pytest → build → smoke の順で全部を通す

## 期待値の出所(HC-016)

| 出所 | 本プロジェクトでの書き方 |
|---|---|
| SPEC の条項 | 寒候年の定義(§2)・値の三種類(§4 L0-2)は条項 ID を書く |
| 外部権威 | 気象庁の公表値。**data/ に取り込んだ値を引く**(原稿に写した定数を書かない) |
| 実測 | 定数で書くときは実測日(2026-09-02)と実測値をコメントに残す |
| 不変量 | 件数は「集合が一致する」「取りこぼしが無い」の形で書く |

## オラクルの出所

| 検査 | 突き合わせる二つ | 循環しない理由 |
|---|---|---|
| G-01 名簿 | S-2 索引 / S-3 PDF の表 / S-4 平年値ページ | 三つとも別の製品。片方から他方を導いていない |
| G-02 資料年数 | S-4 の公表「資料年数」 / S-1 の 1992–2021 で日付が入る年の数 | 平年値ページと記録ページは別系統 |
| G-03 平年値 | S-4 の公表平年値 / S-1 から再計算した平均 | 再計算は**照合にだけ**使い、画面には出さない(SPEC §9) |
| G-05 系列 | S-1(寒候年表記) / S-5(観測年表記・地上気象観測原簿ベース) | 年の付け方が違うので同じ鍵で引けない。写しの比較にならない |
| G-10 目玉 | 観測の傾き / 全山共通の年ラベル置換による帰無分布 | 帰無側は山どうしの相関を保ち、年と日付の対応だけを壊す |
| G-11 推定量 | 最小二乗 / Theil–Sen | 外れ値への感度が違う二つ。符号の一致を見る |

## ケース一覧

### pytest(`tests/test_gates.py`)

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-001 | G-01 | 名簿が 44 山・№ 1..44 の連番・名前が一意 | 一致 |
| T-002 | G-01 | 記録のある山の集合 == 名簿の集合 | 一致 |
| T-003 | G-04 | 値が `YYYY/MM/DD` / `×` / `--` の三種類だけ | 例外 0 件 |
| T-004 | G-04 | **陽性対照** 未知の書式を検出器が捕まえる / **陰性対照** 正当な三種は撃たない | 捕まえる / 撃たない |
| T-005 | G-06 | 全行で「日付が属する寒候年 == 行の寒候年」 | 一致 |
| T-006 | G-06 | 寒候年の境界 4 点(7/31・8/1・12/31・1/1) | SPEC §2 のとおり |
| T-007 | G-06 | 寒候年が昇順で一意 | 一致 |
| T-008 | G-02 | 公表「資料年数」と記録の突合 | 44/44 |
| T-009 | G-03 | 再計算した平均と公表平年値の差 | `--` 無し ≤1 日 / 有り ≤2 日 |
| T-010 | G-03 | `normal_index` が `normal` 文字列と一致・統計期間が 1991〜2020 | 一致 |
| T-011 | G-05 | S-1 × S-5 の突合 | 227 件・不一致 0 |
| T-012 | G-05 | **陽性対照** 二つが写しでないこと(年の表記が違う・1 日ずらすと落ちる) | 成立 |
| T-013 | G-10 | 正の傾きが 30 山以上・`p_positive` と `p_median` が 0.01 以下 | 満たす |
| T-014 | G-11 | Theil–Sen と最小二乗の符号一致が 30 以上 | 満たす |
| T-015 | G-11 | **陽性対照** 傾きを与えた合成系列を両推定量が取り戻す | ±0 で一致 |
| T-016 | G-12 | 置換後の中央傾きの中央値が ±2 日/100 年以内・置換が実際に効いている | 満たす |
| T-017 | G-10 | 公開している傾きが、公開している記録から再現できる | 一致 |
| T-018 | G-10 | 除外の理由が実データに立っている(除外リストが緩みすぎない) | 成立 |
| T-019 | — | `mountains.json` の要約が `records.json` から再現できる | 一致 |
| T-020 | — | `meta.json` の件数が実体と一致 | 一致 |
| T-021 | F-07 | 今季の表が 44 山ぶんあり、山岳名が名簿と一致 | 一致 |

### vitest(`tests-js/`)

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-101 | §2 | 寒候年の境界・起点 0 日・実データの値(2025/10/23 = 83 日目) | 一致 |
| T-102 | — | 月初の目盛りの表が 8 月起点の累計と一致し、合計 365 | 一致 |
| T-103 | — | **陽性対照** `tickLabel` に月初以外を渡すと落ちる | 落ちる |
| T-104 | F-09 | `parseAsof` が実行環境の時間帯に依存しない / **陽性対照** 形が違えば落ちる | 満たす |
| T-105 | — | 連続した年の区間分けが、実データで日付のある年の数と一致 | 一致 |
| T-106 | — | 度数分布が値を取りこぼさない(範囲外も端の階級に入る) | 一致 |
| T-107 | — | 出荷している傾きの中央値が、出荷している傾きから再現できる | 一致 |
| T-108 | F-08 | フッタ規約 **陰性対照** 1 + **陽性対照** 7 + ブランチ名は規約でない 1 | 違反 0 / 落ちる |

### 実ブラウザ検品(`harness/smoke.mjs`・122 項目)

| 群 | 見るもの |
|---|---|
| 全画面 × 明暗 | 見出しがひとつ・フッタが規約どおり・背景がテーマの色・コンソール error 0 |
| 今日の山 | 44 山が並ぶ・しるしが描かれる・並べ替えで先頭が最も早い山になる |
| 山ごと | 点の数が日付のある年の数と一致・記録なしの帯・全記録の行数・ポインタで年が出る |
| 遅くなっている | 棒の本数が山数と一致・外した山の表の行数・帰無モデルの説明・「届かないところ」3 件 |
| 図 | 文字の重なり 0・枠からのはみ出し 0 |
| 幅 320/390/768/1280 | 横に溢れない・縦 16,000px 未満・**フッタの高さ < body の逃げ** |
| 検品器そのもの | **陽性対照** 壊したフッタを落とす・わざと重ねた文字を捕まえる |

### 走らせない検査(意図的)

- 標高に関する検査。標高を扱っていない(SPEC §9)
- 平年値の**自前計算値**に対する検査。画面に出さないので、出荷物の性質ではない
